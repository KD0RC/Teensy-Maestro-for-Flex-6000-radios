void configureEqEvents() {
  // fRig.eq.attach_RX_mode_event(onEq_RX_mode);
  // fRig.eq.attach_RX_63Hz_event(onEq_RX_63Hz);
  // fRig.eq.attach_RX_125Hz_event(onEq_RX_125Hz);
  // fRig.eq.attach_RX_250Hz_event(onEq_RX_250Hz);
  // fRig.eq.attach_RX_500Hz_event(onEq_RX_500Hz);
  // fRig.eq.attach_RX_1000Hz_event(onEq_RX_1000Hz);
  // fRig.eq.attach_RX_2000Hz_event(onEq_RX_2000Hz);
  // fRig.eq.attach_RX_4000Hz_event(onEq_RX_4000Hz);
  // fRig.eq.attach_RX_8000Hz_event(onEq_RX_8000Hz);
  // fRig.eq.attach_TX_mode_event(onEq_TX_mode);
  // fRig.eq.attach_TX_63Hz_event(onEq_TX_63Hz);
  // fRig.eq.attach_TX_125Hz_event(onEq_TX_125Hz);
  // fRig.eq.attach_TX_250Hz_event(onEq_TX_250Hz);
  // fRig.eq.attach_TX_500Hz_event(onEq_TX_500Hz);
  // fRig.eq.attach_TX_1000Hz_event(onEq_TX_1000Hz);
  // fRig.eq.attach_TX_2000Hz_event(onEq_TX_2000Hz);
  // fRig.eq.attach_TX_4000Hz_event(onEq_TX_4000Hz);
  // fRig.eq.attach_TX_8000Hz_event(onEq_TX_8000Hz);
}

void onEq_RX_mode() {
  //Serial.println("onEq_RX_mode() event!");
}

void onEq_RX_63Hz() {
  // Serial.println("onEq_RX_63Hz() event!");
}

void onEq_RX_125Hz() {
  // Serial.println("onEq_RX_125Hz() event!");
}

void onEq_RX_250Hz() {
  // Serial.println("onEq_RX_250Hz() event!");
}

void onEq_RX_500Hz() {
  // Serial.println("onEq_RX_500Hz() event!");
}

void onEq_RX_1000Hz() {
  // Serial.println("onEq_RX_1000Hz() event!");
}

void onEq_RX_2000Hz() {
  // Serial.println("onEq_RX_2000Hz() event!");
}

void onEq_RX_4000Hz() {
  // Serial.println("onEq_RX_4000Hz() event!");
}

void onEq_RX_8000Hz() {
  // Serial.println("onEq_RX_8000Hz() event!");
}

void onEq_TX_mode() {
  //Serial.println("onEq_TX_mode() event!");
}

// void onEq_TX_63Hz() {
  //Serial.println("onEq_TX_63Hz() event!");
// }

// void onEq_TX_125Hz() {
  // Serial.println("onEq_TX_125Hz() event!");
// }

// void onEq_TX_250Hz() {
  // Serial.println("onEq_TX_250Hz() event!");
// }

// void onEq_TX_500Hz() {
  // Serial.println("onEq_TX_500Hz() event!");
// }

// void onEq_TX_1000Hz() {
  // Serial.println("onEq_TX_1000Hz() event!");
// }

// void onEq_TX_2000Hz() {
  // Serial.println("onEq_TX_2000Hz() event!");
// }

// void onEq_TX_4000Hz() {
  // Serial.println("onEq_TX_4000Hz() event!");
// }

// void onEq_TX_8000Hz() {
  // Serial.println("onEq_TX_8000Hz() event!");
// }
